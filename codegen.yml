overwrite: true
schema: http://localhost:4000/graphql
generates:
  ./dist/client.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./dist/client.flow.js:
    plugins:
      - flow
      - flow-resolvers