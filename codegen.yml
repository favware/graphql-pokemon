overwrite: true
schema: http://localhost:4000/graphql
generates:
  ./dist/index.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./dist/index.flow.js:
    plugins:
      - flow
      - flow-resolvers