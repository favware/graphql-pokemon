overwrite: true
schema: https://favware.tech/api
generates:
  ./dist/client.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
  ./dist/client.flow.js:
    plugins:
      - flow
      - flow-resolvers